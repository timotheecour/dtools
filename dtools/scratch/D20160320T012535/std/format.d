void ()()
    if (isDelegate)
immutable()format()() {
appender!(immutable);
}

struct Appender(A)
{
    alias T = ;
    struct Data
    {
        T[] arr;
    }

    this(T)
    {
        new Data;
    }

}

A appender(A)()
{
    Appender!A(null);
}
